# Concept: Split an entire array into single element
# Merge each element with other by comparing if a value is greater or less
# Run time efficiency: O(n(logn)); Space: O(n) as in each step we need to create new two arrays (i.e left, right)